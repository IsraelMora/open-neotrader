[
  {
    "name": "calculate_tranches",
    "description": "Calcula el plan de pirámide (tranches) para una nueva señal de entrada.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "symbol": { "type": "string", "description": "Ticker del activo" },
        "entry_price": { "type": "number" },
        "stop_loss": { "type": "number" },
        "target_price": { "type": "number" },
        "total_size_pct": { "type": "number", "description": "% del capital para la posición completa" },
        "entry_pct": { "type": "number", "description": "% del total para la primera tranche" },
        "add_pct": { "type": "number" },
        "max_tranches": { "type": "integer" },
        "add_trigger_r": { "type": "number" }
      },
      "required": ["symbol", "entry_price", "stop_loss"]
    }
  },
  {
    "name": "evaluate_add",
    "description": "Evalúa si una posición abierta ha alcanzado el nivel para añadir la siguiente tranche.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "symbol": { "type": "string" },
        "current_price": { "type": "number" },
        "entry_price": { "type": "number" },
        "stop_loss": { "type": "number" },
        "tranches_executed": { "type": "integer" },
        "max_tranches": { "type": "integer" },
        "add_trigger_r": { "type": "number" },
        "add_pct": { "type": "number" },
        "total_size_pct": { "type": "number" }
      },
      "required": ["symbol", "current_price", "entry_price", "stop_loss"]
    }
  }
]
