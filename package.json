{
  "name": "neurotrader",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma para agentes de IA especializados en trading",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "db:migrate": "pnpm --filter=@neurotrader/api typeorm migration:run",
    "db:generate": "pnpm --filter=@neurotrader/api typeorm migration:generate"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
