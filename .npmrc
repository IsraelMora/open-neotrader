# Un pnpm-lock.yaml por app (no compartido en la raíz del workspace).
# Permite builds Docker standalone por app (cada Dockerfile copia el lock de su app)
# manteniendo el workspace pnpm + turbo para el desarrollo local.
shared-workspace-lockfile=false
