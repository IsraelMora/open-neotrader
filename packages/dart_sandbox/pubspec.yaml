name: dart_sandbox
description: Executes NeuroTrader Python plugins via subprocess (dart:io Process).
version: 1.0.0

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
